# 젠스 개발팀 요청: '요약 버전 간 계층적 일치성(Hierarchical Consistency)' 강제 기능을 엔진에 넣어주세요

## 📌 문제
간단/표준/상세가 **각각 따로 생성**되어:
- 핵심이 달라지고
- 연결어가 기계적으로 삽입되어 문장이 어색합니다

---

## ✅ 필수 규칙

1. **anchor 문장** (원문에서 가장 핵심 결론 1문장)을 **간단/표준/상세 모두에 반드시 포함**
2. **Snowball 포함 관계 강제**:
   - `standard`는 `brief`를 포함
   - `detail`은 `standard`를 포함
   - (normalize().includes() 기준 True)
3. **첫 문장 연결어 제거** + 문장 간 연결어 1개만 허용 + 중복 접속어 금지
4. **파편 문장 제거/복원** + 종결 어미 학술형 통일
5. 문장 **해시(Hash)로 중복 삽입 방지**
6. 모델이 규칙을 어기면 **서버가 자동으로 수리**:
   - `brief`를 `standard` 앞에 삽입
   - `standard`를 `detail` 앞에 삽입
   - 다시 sanitize

---

## 🛠️ 구현 방식

- **LLM을 3번 호출하지 말고**, 1회 호출로 `{anchor, brief, standard, detail}` JSON을 생성
- 서버에서 `enforceHierarchy()`로 규칙 검증/보정 후 캐싱/반환

---

## 📦 제공 코드

제가 제공한 코드 블록(`hierarchical-enforcer.ts`)을:
- `/api/engine` narrative 요약 생성 루틴에 적용하면 됩니다

---

## ✅ Acceptance Test

```javascript
// 검증 항목:
passes.anchorAll === true  // anchor가 3버전 모두 포함
passes.snowball === true   // standard ⊃ brief, detail ⊃ standard
```

**이 두 조건이 항상 `true`가 되도록 보장해주세요.**

---

## 📂 적용 지점

**파일**: `/api/engine` (narrative 요약 생성 루틴)

**적용 순서**:
1. `hierarchical-enforcer.ts`를 프로젝트에 추가
2. `buildHierarchicalNarrativePrompt()` 사용해 프롬프트 생성
3. Gemini에 1회 호출로 `{anchor, brief, standard, detail}` JSON 생성
4. `enforceHierarchy()`로 검증/보정
5. 결과 반환

---

## 📊 예상 산출물 (JSON)

```json
{
  "anchor": "본 연구는 영재 학습자의 개인차를 고려한 맞춤형 교수 전략이 필요함을 강조한다.",
  "brief": "본 연구는 영재 학습자의 개인차를 고려한 맞춤형 교수 전략이 필요함을 강조한다. 학습유형과 학습전략 간의 관계 분석을 통해 개별화된 교육적 접근이 학업성취도 향상에 기여할 것으로 기대된다.",
  "standard": "본 연구는 영재 학습자의 개인차를 고려한 맞춤형 교수 전략이 필요함을 강조한다. 학습유형과 학습전략 간의 관계 분석을 통해 개별화된 교육적 접근이 학업성취도 향상에 기여할 것으로 기대된다. 모든 학습자는 학습 방식과 전략에 있어 다양한 개인차가 존재하며, 특히 영재는 더 다양한 학습 요구를 나타낸다. 이러한 요구에 부응하기 위해 진단을 통한 교수적 처방은 영재들에게 꼭 필요한 활동이다.",
  "detail": "본 연구는 영재 학습자의 개인차를 고려한 맞춤형 교수 전략이 필요함을 강조한다. 학습유형과 학습전략 간의 관계 분석을 통해 개별화된 교육적 접근이 학업성취도 향상에 기여할 것으로 기대된다. 모든 학습자는 학습 방식과 전략에 있어 다양한 개인차가 존재하며, 특히 영재는 더 다양한 학습 요구를 나타낸다. 이러한 요구에 부응하기 위해 진단을 통한 교수적 처방은 영재들에게 꼭 필요한 활동이다. 학습유형은 학습전략 선택에 큰 영향을 미치므로 영재의 습관적인 유형과 의도적인 전략 선택을 분석할 필요가 있다. 결과적으로 학습자가 자신의 특성을 알고 전략을 의식적으로 사용할 때 학업성취도가 향상될 것이라는 연구를 기반으로 본 과업을 수행하고자 한다. 연구에서는 성별, 학년, 초등·중학 구분 등의 변인이 학습유형과 전략 선택에 미치는 영향을 비교 분석하며, 영재교육 현장에서 실질적으로 활용 가능한 교수학습 모델을 제시한다.",
  "meta": {
    "ratios": { "brief": 0.15, "standard": 0.30, "detail": 0.55 },
    "passes": { "anchorAll": true, "snowball": true, "sanitized": true }
  }
}
```

---

## 🚀 기대 효과

- **간단→표준→상세**가 "뼈대 유지"된 채로 확장됨
- 사용자가 어떤 버전을 봐도 **핵심 내용이 일관됨**
- 연결어/파편 문제 자동 해결
- **UI에서 탭 전환 시 혼란 제거**

---

**요청 완료!** 🎉
